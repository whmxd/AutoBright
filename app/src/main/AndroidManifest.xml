<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wutong.autobright">

    <!--  修改系统设置  -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoBright">

        <!--    小米 取消"读取应用列表权限"    -->
        <meta-data android:name="do_not_need_get_installed_apps" android:value="true"/>

        <!-- 标题栏图标按钮 -->
        <service
            android:name="com.wutong.autobright.TileService"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/app_icon_close"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>

</manifest>